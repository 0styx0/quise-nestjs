type StripeLineItem {
  id: ID!
  name: String!
  priceUnit: Int!
}

type StripeCheckoutSession {
  id: ID!
  priceTotal: Int!
  lineItems: [StripeLineItem!]!
}

type Query {
  fetchOrder(paymentKey: String!): StripeCheckoutSession!
}